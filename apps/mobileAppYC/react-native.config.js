/**
 * React Native CLI configuration for linking custom fonts.
 * `npx react-native-asset` uses this file to copy assets into iOS and Android builds.
 */
module.exports = {
  assets: ['./assets/fonts'],
  project: {
    ios: {},
    android: {},
  },
};
